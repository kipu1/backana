spring.application.name=Nutriologa-Analia-Roman

# Configuración de PostgreSQL con variables de entorno
spring.datasource.url=postgresql://postgres:sxftgYNJuwsRgGVBdTOZsJkqMLHxfuTt@postgres.railway.internal:5432/railway
spring.datasource.username=postgres
spring.datasource.password=sxftgYNJuwsRgGVBdTOZsJkqMLHxfuTt
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de archivos (comentada porque no puedes usar rutas locales en Render)
# spring.servlet.multipart.location=/path/to/cloud/storage
# file.upload-dir=/path/to/cloud/storage

# Configuración de multipart para subida de archivos
spring.mvc.static-path-pattern=/archivos/**
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Configuración de JPA
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Stripe (usa la clave secreta desde variables de entorno)
stripe.api.key=${STRIPE_API_KEY}

# Configuración adicional
spring.main.allow-circular-references=true
logging.level.org.springframework.security=DEBUG
